<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- v1.5 · EL PERMISO QUE FALTABA. Capacitor, cuando la pagina de dentro pide el
         microfono, exige RECORD_AUDIO **y** MODIFY_AUDIO_SETTINGS. Este segundo no estaba
         declarado, y un permiso que no se declara NUNCA cuenta como concedido: por eso el
         WebView denegaba el microfono a la web (NotAllowedError) aunque en los ajustes del
         telefono el microfono de Azkarin estuviera en "Permitir". Es un permiso normal: no
         pregunta nada al instalar ni al usarlo. -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- v1.4: para que Azkarin sepa DESDE DONDE sales y pueda decir cuanto tardas.
         El WebView solo puede dar la ubicacion si la app pide estos permisos. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- v1.7 · para que al oir «Azkarin» pueda abrirse encima del bloqueo o de otra app
         (desde Android 10 un servicio no puede abrir pantallas: hace falta la notificacion
         a pantalla completa, como una llamada entrante) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- v1.10 · para que la escucha arranque sola al encender el movil y no haya que
         activarla nunca (activarla HABLANDO cuando esta apagada es imposible: si no
         escucha, no puede oir que le llaman) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- v1.14: para abrir el instalador con la APK que se acaba de bajar -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- v1.12 · EL TIMBRE DE GOOGLE. Asier: «¿no podemos aprovechar que tenemos a Google,
             hacer un puente para que funcione como el Hey Google?». Si: este segundo icono,
             «Hablar con Azkarin», abre la app YA en modo conversacion. Con una rutina del
             Asistente («cuando digo Azkarin -> abrir Hablar con Azkarin») o diciendo
             «Hey Google, abre Hablar con Azkarin», el que escucha la palabra es el chip de
             Google (que no gasta) y nuestra escucha se puede apagar del todo. -->
        <activity-alias
            android:name=".HablarActivity"
            android:targetActivity=".MainActivity"
            android:label="Hablar con Azkarin"
            android:icon="@mipmap/ic_launcher"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity-alias>

        <service
            android:name=".WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <receiver
            android:name=".ArranqueReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
